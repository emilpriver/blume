---
"blume": patch
---

Treat numeric-prefixed index files (`01-index.mdx`) as directory indexes everywhere routing already does: they now sort first in the sidebar, keep their group's route path intact, and resolve relative links against their own route.
