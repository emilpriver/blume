---
"blume": patch
---

The Notion source's `publishedValue` now defaults to `Published` as documented: pages whose Status/select property holds any other value import as drafts, while pages without a Status property stay published.
