---
"blume": patch
---

The closed Ask AI panel is now `inert`, so its buttons and textarea no longer sit in the keyboard tab order on every page, and closing the panel returns focus to the element that opened it.
