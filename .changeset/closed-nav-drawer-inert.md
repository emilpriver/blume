---
"blume": patch
---

Closed mobile nav drawers are now `inert` and `aria-hidden`, so their links drop out of the keyboard tab order while closed; the docs sidebar stays fully interactive at desktop widths where it becomes a static column.
