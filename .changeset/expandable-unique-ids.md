---
"blume": patch
---

Multiple `<Expandable>` (or same-titled accordion) components on one page no longer render duplicate ids; later duplicates get a numeric suffix while the first keeps the plain slug so hash deep-links stay stable.
